#include <stdio.h>

int main(void) {
    int t;
    scanf("%d",&t);
    for(int i=0;i<t;i++)
    {
        int  t1,t2,r1,r2;
        scanf("%d%d%d%d",&t1,&t2,&r1,&r2);
        float(a)=(float)(t1*t1)/(r1*r1*r1);
        printf("%f\n",a);
        float(b)=(float)(t2*t2)/(r2*r2*r2);
          printf("%f\n",b);
        //  if(a==b)
        //   printf("Yes\n");
        //  else
        //  printf("No\n");
    }
	return 0;
}

